//messenger-bot
const id = 'FB-MSGER-ID'
const secret = 'FB-MSGER-SECRET'

//imgur
const user = 'IMGUR-ID'
const pw = 'IMGUR-PW'

//firebase
const firebase_config = {
    apiKey: "API",
    authDomain: "domain",
    databaseURL: "url",
    storageBucket: "bucket",
  };

module.exports = { id, secret, user, pw, firebase_config}
